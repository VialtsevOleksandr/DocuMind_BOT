# 📄 DocuMind AI Bot

**DocuMind** — це інтелектуальний Telegram-бот для аналізу документів, який поєднує потужність Google Cloud Vision API та Gemini AI для автоматичного розпізнавання тексту з фотографій та його інтелектуальної обробки.

---

## 🎯 Суть проєкту

DocuMind — це персональний AI-асистент для роботи з документами безпосередньо у Telegram. Замість того, щоб вручну переписувати текст з паперових документів, рахунків, договорів чи нотаток, користувач просто фотографує документ і отримує:

- **Автоматичне розпізнавання тексту** (OCR) з будь-якого зображення
- **Інтелектуальний аналіз** за допомогою AI (резюме, ключові моменти, переклад)
- **Контекстні відповіді** на конкретні питання про документ
- **Структуровану інформацію** замість хаотичного тексту

Це як мати особистого аналітика, який миттєво "читає" ваші документи і пояснює найважливіше.

---

## ✨ Ключові можливості

### 1. **Два режими роботи**

#### 📸 Режим меню (Інтерактивний)
Просто надішліть фото — бот розпізнає текст і запропонує меню дій:
- 📝 **Стислий зміст** — розумне резюме документу
- 🇬🇧 **Переклад на англійську** — з адаптацією форматів
- 🇺🇦 **Переклад на українську** — теж з адаптацією форматів
- 🔑 **Ключові моменти** — структурована витяжка даних

#### 💬 Режим прямого запиту
Надішліть фото **з підписом** (наприклад: _"Скільки треба заплатити?"_) — бот відразу дасть відповідь на ваше питання без меню.

### 2. **Адаптивний аналіз**

Gemini AI автоматично визначає тип документу та адаптує відповідь:
- **Фінансові документи** (рахунки, чеки) → сума, дедлайн, призначення платежу
- **Договори** → сторони угоди, предмет, ризики, терміни
- **Листи/Статті** → executive summary, основні тези
- **Інструкції** → покрокові дії
- **Рукописні нотатки** → структурований текст

### 3. **Обробка великих текстів**

Якщо розпізнаний текст або результат аналізу перевищує 3000 символів, бот автоматично створює `.txt` файл для зручності.

### 4. **Кешування даних**

Розпізнаний текст зберігається у Firestore, що дозволяє:
- Швидко повертатися до попередніх документів
- Виконувати різні операції без повторного OCR
- Економити API-запити та час

---

## 🏗️ Архітектура проєкту

### Технологічний стек

| Компонент | Технологія | Призначення |
|-----------|-----------|-------------|
| **Bot Framework** | `python-telegram-bot` | Взаємодія з Telegram API |
| **OCR Engine** | Google Cloud Vision API | Розпізнавання тексту з зображень |
| **AI Brain** | Google Gemini 2.5 Flash | Інтелектуальний аналіз документів |
| **Database** | Google Firestore | Кешування розпізнаного тексту |
| **Secrets** | Google Secret Manager | Безпечне зберігання API-ключів |
| **Runtime** | Google Cloud Functions | Serverless виконання (webhook) |

### Логіка роботи

```
┌─────────────┐
│  Користувач │
│  (Telegram) │
└──────┬──────┘
       │ 📸 Надсилає фото (± підпис)
       ▼
┌─────────────────────┐
│  Telegram Webhook   │
│  (Cloud Function)   │
└──────┬──────────────┘
       │
       ├─► Vision API ───► OCR текст
       │
       ├─► Firestore ───► Збереження/Кеш
       │
       └─► Gemini AI ───► Аналіз + Відповідь
              │
              ▼
       ┌──────────────┐
       │  Markdown     │
       │  Response     │
       └──────────────┘
```

---

## 🧠 AI-Промпти та їх магія

Проєкт використовує **інженерію промптів** для отримання структурованих відповідей. Кожна функція має власний системний промпт:

### 📝 Summarize (Резюме)
- Визначає тип документу
- Адаптує формат відповіді під контекст
- Виділяє action items (що треба зробити)

### 🌍 Translate (Переклад)
- Зберігає структуру оригіналу
- Адаптує формати дат/валют
- Виправляє помилки OCR перед перекладом

### 🔑 Keywords (Ключові моменти)
- Групує факти за сутностями
- Створює ієрархічні списки
- Уникає дублювання інформації

### 💬 Custom (Довільний запит)
- Відповідає на конкретне питання користувача
- Використовує контекст документу

---

## 📂 Структура коду

```
main.py
├── 1. Конфігурація (PROJECT_ID, MODEL_NAME, логування)
├── 2. AI-Промпти (SYSTEM_PROMPTS)
├── 3. Secret Manager (отримання API-ключів)
├── 4. Ініціалізація клієнтів (Vision, Firestore, Gemini, Bot)
├── 5. UI-Клавіатури (меню дій, кнопка "Назад")
├── 6. Core Logic
│   ├── real_vision_api() — OCR через Vision API
│   ├── real_gemini_api() — аналіз через Gemini
│   ├── save_to_cache() — збереження у Firestore
│   └── get_from_cache() — отримання з Firestore
├── 7. Helper Functions
│   ├── safe_edit_message() — безпечне редагування з Markdown
│   └── send_smart_response() — автоматичне створення файлу для великих текстів
├── 8. Bot Handlers
│   ├── start_command() — привітання
│   ├── clear_command() — очищення сесії
│   ├── process_photo_interactive() — фото без підпису → меню
│   ├── process_photo_direct() — фото з підписом → пряма відповідь
│   └── process_callback() — обробка натискань кнопок
└── 9. Entry Point (telegram_webhook для Cloud Functions)
```

---

## 🎨 UX/UI Features

### Інтерактивні кнопки
- **Головне меню** — 4 типові операції + очищення
- **Кнопка "Назад"** — повернення до меню після аналізу
- **"Всі дії з документом"** — перехід до меню після прямого запиту
- **"Нове фото"** — скидання сесії

### Статус-повідомлення
- `⏳ Аналізую зображення...` — під час OCR
- `🧠 Gemini працює...` — під час AI-обробки
- `✅ Текст розпізнано!` — успішне завершення

### Обробка помилок
- `⚠️ Текст не виявлено` — якщо OCR не знайшов текст
- `❌ Помилка при обробці` — критичні збої
- `⚠️ Сесія застаріла` — якщо кеш не знайдено

---

## 🔐 Безпека

### Secret Management
Всі чутливі дані зберігаються у **Google Secret Manager**:
- `TELEGRAM_BOT_TOKEN` — токен бота
- `GEMINI_API_KEY` — ключ для Gemini AI

Локально для тестування можна використовувати змінні середовища.

---

## 🚀 Особливості реалізації

### Асинхронність
Весь код використовує `async/await` для неблокуючих операцій:
- Паралельне завантаження фото та виклик API
- Швидка відповідь користувачу без затримок

### Обробка Markdown
Спеціальна функція `safe_edit_message()` захищає від помилок парсингу:
- Автоматично вимикає Markdown при помилках
- Додає попередження про відключене форматування

### Робота з великими текстами
Функція `send_smart_response()`:
- Автоматично створює `.txt` файл, якщо текст > 3000 символів
- Зберігає форматування у файлах
- Додає зручні кнопки навігації

### Кешування
- **Ключ кешу:** `{chat_id}_{message_id}` — унікальний для кожного повідомлення
- **TTL:** без обмежень (можна додати автоматичне очищення)
- **Мета:** уникнути повторних OCR-запитів для одного документу

---

## 📊 Використані моделі

### Gemini 2.5 Flash
- **Швидкість:** ~1-2 секунди на запит
- **Контекст:** до 1 млн токенів
- **Мова:** повна підтримка української
- **Формат:** Markdown-friendly відповіді

### Vision API
- **Точність:** до 99% для друкованого тексту
- **Підтримка:** 50+ мов, включно з українською
- **Формати:** JPEG, PNG, PDF, TIFF

---

## 💡 Приклади використання

### Сценарій 1: Аналіз рахунку
1. Фотографуєте рахунок на оплату
2. Надсилаєте фото без підпису
3. Натискаєте "📝 Стислий зміст"
4. Отримуєте: суму, дедлайн, призначення платежу

### Сценарій 2: Швидке питання
1. Фотографуєте договір
2. Додаєте підпис: _"Які ризики у цьому договорі?"_
3. Бот одразу аналізує і відповідає

### Сценарій 3: Переклад документу
1. Фото документу українською
2. Кнопка "🇬🇧 English"
3. Отримуєте професійний переклад з адаптованими датами

---

## 🎓 Навчальна цінність

Проєкт демонструє:
- ✅ Інтеграцію з Google Cloud Services
- ✅ Роботу з Telegram Bot API
- ✅ Інженерію промптів для LLM
- ✅ Асинхронне програмування на Python
- ✅ Serverless архітектуру
- ✅ NoSQL базу даних (Firestore)
- ✅ Обробку помилок та edge cases
- ✅ UX-дизайн для чат-ботів

---

## 👤 Автор

**Вяльцев Олександр**  
Київський національний університет імені Тараса Шевченка  
Факультет комп'ютерних наук та кібернетики  
Група ТТП-42

---

Лабораторна робота №2 з дисципліни "Хмарні обчислення", 2025
